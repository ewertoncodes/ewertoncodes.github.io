---
layout: post
title: Post de Teste - Recursos do Blog
date: 2026-05-06 10:00:00 -0300
categories:
- dev
tags:
- ruby
- sql
- go
description: Post de teste demonstrando snippets de código, fórmulas matemáticas e outros recursos do blog.
---

Este post serve para testar os recursos de formatação do blog.

## Código Ruby

```ruby
class Pessoa
  attr_accessor :nome, :idade

  def initialize(nome, idade)
    @nome = nome
    @idade = idade
  end

  def maior_de_idade?
    @idade >= 18
  end

  def to_s
    "#{@nome} (#{@idade} anos)"
  end
end

pessoa = Pessoa.new("Ewerton", 30)
puts pessoa
puts pessoa.maior_de_idade? # => true
```

## Código SQL

```sql
SELECT
  u.name,
  COUNT(p.id) AS total_posts,
  AVG(p.views) AS media_views
FROM users u
LEFT JOIN posts p ON p.user_id = u.id
WHERE u.created_at >= NOW() - INTERVAL '30 days'
GROUP BY u.id, u.name
HAVING COUNT(p.id) > 0
ORDER BY total_posts DESC
LIMIT 10;
```

## Código Go

```go
package main

import (
    "fmt"
    "net/http"
)

func handler(w http.ResponseWriter, r *http.Request) {
    fmt.Fprintf(w, "Hello, World!")
}

func main() {
    http.HandleFunc("/", handler)
    fmt.Println("Servidor rodando na porta 8080")
    http.ListenAndServe(":8080", nil)
}
```

## Código JavaScript

```javascript
const fetchPosts = async (userId) => {
  try {
    const response = await fetch(`/api/users/${userId}/posts`);
    if (!response.ok) throw new Error("Erro ao buscar posts");
    const data = await response.json();
    return data;
  } catch (error) {
    console.error(error);
    return [];
  }
};
```

## Código Bash

```bash
#!/bin/bash

# Deploy simples
echo "Iniciando deploy..."
git pull origin main
bundle exec jekyll build
echo "Deploy concluído!"
```

## Fórmulas Matemáticas

Fórmula inline: A complexidade do algoritmo é $O(n \log n)$.

Fórmula em bloco — Teorema de Bayes:

$$
P(A \mid B) = \frac{P(B \mid A) \cdot P(A)}{P(B)}
$$

Equação quadrática:

$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

## Tabela

| Algoritmo      | Melhor caso  | Caso médio   | Pior caso    |
|----------------|--------------|--------------|--------------|
| Bubble Sort    | $O(n)$       | $O(n^2)$     | $O(n^2)$     |
| Merge Sort     | $O(n\log n)$ | $O(n\log n)$ | $O(n\log n)$ |
| Quick Sort     | $O(n\log n)$ | $O(n\log n)$ | $O(n^2)$     |
| Binary Search  | $O(1)$       | $O(\log n)$  | $O(\log n)$  |

## Blocos de destaque

> **Dica:** Use `bundle exec jekyll serve --livereload` para ver as mudanças em tempo real.
{: .prompt-tip }

> **Atenção:** Nunca faça push direto na branch main em produção.
{: .prompt-warning }

> **Perigo:** Nunca exponha sua `DEVTO_API_KEY` no código.
{: .prompt-danger }

> **Info:** O Chirpy suporta MathJax para fórmulas matemáticas nativamente.
{: .prompt-info }

## Lista de tarefas

- [x] Configurar o blog com Chirpy
- [x] Migrar posts antigos
- [x] Automatizar publicação no dev.to
- [ ] Escrever mais posts
- [ ] Configurar comentários com Giscus
